spring:
  application:
    name: lwx-magic-code-backend
  # mysql
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/lwx_magic_code
    username: root
    password: 2909314910
server:
  port: 8123
  servlet:
    context-path: /api
# springdoc-openapi
springdoc:
  group-configs:
    - group: 'default'
      paths-to-match: '/**'
      packages-to-scan: com.lwx.lwxmagiccodebackend.controller
# knife4j
knife4j:
  enable: true
  setting:
    language: zh_cn